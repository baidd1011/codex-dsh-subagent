/**
 * Bin-only package for the stdio DSH MCP server. The selected `cordis.yml`
 * owns the runtime composition; this module intentionally exports no plugin.
 * @module @deepseek-ai/dsh-mcp-agent-demo
 */

export {}
